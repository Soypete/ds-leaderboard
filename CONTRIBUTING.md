# Contributing

The full guide lives at [docs/CONTRIBUTING.md](docs/CONTRIBUTING.md) — dev
setup (`npm run dev:up` for the local Supabase stack), the receipt-mirror
contract with the game repo, and testing expectations (`TESTING.md`).

Quick pre-PR check: `npm run typecheck && npm run lint && npm test && npm run build`.
